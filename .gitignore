# build output
dist/
# dependencies
node_modules/
# astro
.astro/
# logs
npm-debug.log*
# environment
.env
.env.production
# macOS / editor
.DS_Store
.vscode/

# lock file omitted: npm cannot generate complete cross-platform lock on Windows
# (Cloudflare/Linux runs npm install instead of npm ci)
package-lock.json
